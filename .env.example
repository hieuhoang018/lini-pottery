# Compose/build-time only. App runtime config still lives in server/.env and client/.env.
VITE_API_URL=/api

# Only used if you run: docker compose --profile local-db up
POSTGRES_USER=lini
POSTGRES_PASSWORD=change-me
POSTGRES_DB=lini
